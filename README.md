# liq-store
A core infrastructure component of the Latent Infinity Quant (LIQ) ecosystem, `liq-store` provides a backend-agnostic storage and caching layer. Both `liq-data` (raw market data) and `liq-features` (computed indicators) use this library for persistent storage.
